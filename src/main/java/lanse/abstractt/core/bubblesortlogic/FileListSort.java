package lanse.abstractt.core.bubblesortlogic;

import lanse.abstractt.core.bubble.Bubble;
import lanse.abstractt.core.displaylogic.DisplayModeSelector;

public class FileListSort {

    public static void sort(boolean isFunctionBubble, Bubble[] bubbles) {
        if (DisplayModeSelector.displayMode == DisplayModeSelector.DisplayMode.FILE_LIST) return;

        if (!isFunctionBubble){

        } else {

        }
    }
}
